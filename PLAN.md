# timetable-web PLAN

---

### Milestone 1: 프로젝트 초기화

**목표:** Next.js + TypeScript + Tailwind CSS 프로젝트 셋업

> 📖 TECHSPEC 섹션 4.1 참조

**완료 조건:** `npm run dev`로 로컬 서버 실행 시 기본 페이지 표시 ✅

- Commits: d05a37b, 9eeb709

---

### Milestone 2: Google Sheets 준비

**목표:** 시간표 시트 생성 + 서비스 계정 설정

**작업 (수동)**

- [x] Google Cloud Console에서 프로젝트 생성
- [x] Google Sheets API 활성화
- [x] 서비스 계정 생성 + JSON 키 다운로드
- [x] 시간표 Google Sheets 생성 (실제 시간표 구조로 샘플 데이터 입력)
- [x] 시트를 서비스 계정 이메일에 "뷰어" 권한으로 공유
- [x] `.env.local`에 `GOOGLE_SERVICE_ACCOUNT_KEY`, `GOOGLE_SHEET_ID` 설정

**완료 조건:** 서비스 계정으로 시트 데이터 접근 가능 확인 (API Explorer 또는 curl) ✅

- Commits: 수동 작업 (코드 커밋 없음)

---

### Milestone 3: Google Sheets API 연동

**목표:** 서비스 계정으로 Google Sheets API 데이터 페칭

> 📖 TECHSPEC 섹션 7.1 참조

**Unit Tests**

- [x] 환경변수 미설정 시 명확한 에러 발생
- [x] API 응답을 받아 raw 데이터 반환 확인

**완료 조건:** `fetchTimetableData()` 호출 시 실제 시트 데이터(raw)가 콘솔에 출력됨 ✅

- Commits: eadc918, 8b69f8f

---

### Milestone 4: 시트 데이터 파싱

**목표:** 셀 값, 배경색, 병합 정보를 내부 데이터 모델(TimetableData)로 변환

> 📖 TECHSPEC 섹션 6.1, 7.2, 7.3 참조

**완료 조건:** 파싱 결과가 TimetableData 타입과 일치하고, 셀 값·배경색·병합 정보가 정확함 ✅

- Commits: 89c036d, 7ce3def

---

### Milestone 4-T: 파싱 P0 테스트

**목표:** 파싱 로직과 색상 변환의 정확성 검증

> 📖 TECHSPEC 섹션 8.1 #2, #3 참조

**Unit Tests (P0)**

- [x] RGB(0~1) → hex 변환 정확성 (예: `{red: 0.66, green: 0.84, blue: 0.63}` → `#a8d6a1`)
- [x] 배경색 없는 셀 → `#ffffff` 반환
- [x] 병합 셀 처리: rowSpan 값 정확성 (2시간 수업 → rowSpan: 2)
- [x] 병합 연속 셀: isMergedContinuation = true
- [x] 헤더 영역 파싱: programName, period, location, totalHours 추출
- [x] 빈 셀 처리

**완료 조건:** `npm test` 전체 통과 ✅

- Commits: d808784, 02221e8

---

### Milestone 5: 시간표 그리드 렌더링

**목표:** 헤더 정보 + 요일 × 시간대 테이블 + 셀 병합 + 배경색 적용

> 📖 TECHSPEC 섹션 1.1, 7.2, 7.3 참조

**Manual Tests**

- [x] 헤더에 프로그램명, 기간, 장소, 이수시간 표시
- [x] 요일(화~월) × 시간대(09:00~17:00) 그리드 렌더링
- [x] 각 셀에 수업명 + 부가정보 표시
- [x] 셀 배경색이 시트와 동일하게 적용
- [x] 2시간 연속 수업이 병합된 셀로 표시 (rowSpan)
- [x] 모바일 뷰포트(375px)에서 테이블이 가로 스크롤로 표시 + 시간 컬럼 좌측 고정

**완료 조건:** 브라우저에서 `/` 접속 시 시트와 동일한 형태의 시간표 그리드 표시 ✅

- Commits: 2d07209, e4c5268, e43bd87, a6dd6d4

---

### Milestone 6: 첫 배포

**목표:** Vercel 배포 + 환경변수 설정

> 📖 TECHSPEC 섹션 4.3 참조

**Manual Tests**

- [x] 배포된 URL에서 시간표 정상 표시
- [x] Google Sheets API 연동 정상 동작 (서비스 계정 환경변수)
- [x] 카카오톡 인앱 브라우저에서 정상 렌더링

**완료 조건:** 공유 가능한 URL에서 시간표 열람 가능 ✅

- Commits: M5 이후 수동 배포 완료

---

### Milestone 7: 회차 탭 전환

**목표:** 회차(시트 탭) 간 클라이언트 탭 전환 + 현재 회차 자동 선택

> 📖 TECHSPEC 섹션 7.4, 7.8 참조

**Manual Tests**

- [x] 탭 클릭 시 페이지 새로고침 없이 회차 전환
- [x] 각 회차 내 주차는 한 페이지에 모두 렌더링
- [x] 오늘 날짜에 해당하는 회차가 기본 선택됨
- [x] 교육 기간 외 접속 시 첫 번째 회차 표시

**완료 조건:** 회차 탭 클릭으로 즉시 전환되고, 초기 로드 시 현재 회차 자동 선택 ✅

- Commits: ba6afc7, a40702f, 65ef2d3

---

### Milestone 8: 셀 폰트 색상 반영

**목표:** 시트 셀의 폰트 색상을 웹에 동일하게 적용. 공휴일/특수일은 관리자가 셀에 빨간 폰트로 직접 타이핑하여 자동으로 구분된다 (별도 공휴일 검출 로직 없음).

**Unit Tests**

- [x] `toTextColor`: RGB(0~1) → hex 변환, 미지정 시 `#000000`
- [x] `parseGridSlots`: 셀 폰트 색상을 `textColor`로 추출

**Manual Tests**

- [x] 시트에서 빨간 폰트로 작성된 셀(공휴일명 등)이 브라우저에도 빨갛게 표시됨

**완료 조건:** 시트 폰트 색상 변경이 웹에 그대로 반영됨 ✅

- Commits: a6e5378

---

### Milestone 11: PIN 접근 제어

**목표:** 공유 PIN으로 시간표 접근 제한 — Upstash Redis에 PIN 저장, 미인증 시 PIN 입력 페이지로 리다이렉트

> 📖 TECHSPEC 섹션 7.5 참조
> 📖 notes/redis-upstash-supabase.md, notes/cookies.md 참조

**Unit Tests**

- [x] PIN 일치 시 쿠키 설정 + 200 응답
- [x] PIN 불일치 시 401 응답

**Implementation**

- [x] `@upstash/redis` 설치
- [x] `lib/pin.ts` — `getStoredPin()` (Upstash에서 `student_pin` 키 조회)
- [x] `app/api/auth/pin/route.ts` — POST 핸들러 (PIN 검증 + 쿠키 설정)
- [x] `app/pin/page.tsx` — PIN 입력 페이지 (Client Component, form submit)
- [x] `proxy.ts` — 쿠키 검증 + 미인증 시 `/pin` 리다이렉트 (Next.js 16에서 middleware→proxy 전환)

**KV 세팅 (수동 작업 — Vercel KV로 대체)**

- [x] Vercel 대시보드 → Storage → KV 생성 (리전: Washington DC)
- [x] `.env.local`에 `UPSTASH_REDIS_REST_URL`, `UPSTASH_REDIS_REST_TOKEN` 추가
- [x] curl로 초기 PIN 설정: `SET student_pin 1234`
- [x] Vercel 프로젝트 환경변수에도 동일한 두 값 주입 (배포 환경용)

**Manual Tests**

- [x] 쿠키 없이 `/` 접속 → `/pin`으로 리다이렉트
- [x] 올바른 PIN 입력 → 시간표 페이지로 이동 + `student_pin` 쿠키 생성 확인 (DevTools Application 탭)
- [x] 잘못된 PIN 입력 → "PIN이 올바르지 않습니다." 에러 메시지 표시
- [x] PIN 인증 후 재접속 시 PIN 재입력 불필요 (쿠키 자동 전송)
- [x] Upstash 콘솔에서 PIN 변경 후 기존 쿠키로 접근 → `/pin` 리다이렉트 (진실원 분리 검증)

**완료 조건:** PIN 미인증 사용자는 시간표 접근 불가, 인증 후 쿠키로 자동 인증, Upstash에서 PIN 변경 시 기존 세션 즉시 무효화

- Commits: b578757, dfae5f7, bfdcb34, 35688fa, 19f20a8, 5e31d88

---

### Milestone 12: 관리자 인증

**목표:** 관리자 비밀번호 인증 (환경변수) + 쿠키 기반 세션 + `/admin` 라우트 가드

> 📖 TECHSPEC 섹션 7.6 참조

**Unit Tests**

- [x] 관리자 비밀번호 일치 시 쿠키 설정 + 200 응답
- [x] 관리자 비밀번호 불일치 시 401 응답

**Manual Tests**

- [x] `/admin` 미인증 접속 → 로그인 페이지 표시
- [x] 올바른 비밀번호 입력 → 관리자 페이지 진입
- [x] 인증 후 재접속 시 쿠키로 자동 인증

**완료 조건:** 관리자 비밀번호로 `/admin` 접근 가능, 쿠키 만료 전까지 자동 인증

- Commits: b548f01, 019ebaf, b883f0d

---

### Milestone 13: 관리자 기능

**목표:** 관리자 페이지에서 PIN 변경 (KV) + 시간표 최신화 (revalidatePath) + 공지 작성/삭제 (KV)

> 📖 TECHSPEC 섹션 7.6, 7.7 참조

**Manual Tests**

- [x] PIN 변경 → 기존 PIN으로 접근 실패, 새 PIN으로 접근 성공
- [x] 시트 수정 → 관리자 페이지의 최신화 버튼 클릭 → 메인 페이지에 변경 반영
- [x] 공지 작성 → 시간표 상단에 공지 표시
- [x] 공지 삭제 → 공지 사라짐 확인

**완료 조건:** 관리자 페이지에서 PIN 변경, 시간표 최신화, 공지 CRUD 모두 동작

- Commits: 2272461, 92181d3, 28ba205, 5762d0d, a0c4768, 8bf03f6

---

### Milestone 14: 최종 QA

**목표:** 에러 처리 UI 적용 + 전체 테스트 검증

> 📖 TECHSPEC 섹션 1.4, 8.1, 8.2 참조

**Manual Tests**

- [x] PIN 보호 동작 확인 (배포 환경)
- [x] 관리자 기능 동작 확인 (배포 환경)
- [x] API 에러 시 에러 메시지 표시: "시간표를 불러오는 중 오류가 발생했습니다. 잠시 후 다시 시도해주세요."

**완료 조건:** 전체 기능 최종 확인, 배포 환경에서 정상 동작

- Commits: 2005eb9

**완료 조건 충족** ✅

---

### Milestone 15: QA 기반 UI 개선 (진행 중)

**목표:** 실사용 QA 피드백을 받아 UI를 점진적으로 개선한다. 단일 기능 단위로 작고 자주 커밋/배포를 반복한다.

**운영 방식**

- QA 피드백을 받을 때마다 아래 로그에 항목을 추가한다 (형식: `- [ ] 피드백 요약`)
- 항목 하나가 완료되면 해당 체크박스를 표시하고 `→ 커밋해시` 를 덧붙인다
- 마일스톤은 **닫히지 않는다** — 운영 기간 내내 계속 새 항목이 추가된다

**QA 개선 로그**

- [x] 공지 박스 디자인 개선 (amber 톤 + 좌측 강조 바 + 📢 이모지/"공지" 라벨) → 5aa9a85
- [x] 하단 네비게이션 링크 가시성 개선 (이모지 + 진한 회색 + 상하 패딩 통일) → 5aa9a85
- [ ] 모달 닫기 애니메이션 추가

**완료 조건:** 없음 (운영 중 계속 반복)

- Commits: 5aa9a85

---

### Milestone 16: 시트 구조 재설계 (단일 탭 · 회차 블록)

**목표:** 한 탭에 가로로 반복되는 회차 블록(6열 고정 + 1열 간격)을 파싱하고, 지정된 탭 하나만 노출하며, 오늘 날짜 이전 ~ 현재 회차까지만 렌더링한다.

**배경**
- 매니저 실제 시트는 한 탭에 1~5회차 시간표가 가로로 나열됨 (A~F 1회차, G 빈 열, H~M 2회차, ...)
- 학생은 오늘 날짜가 포함된 회차까지만 볼 수 있어야 함
- 다른 탭(시트) 정보는 학생에게 노출되지 않아야 함

**가정**
- 블록 크기는 **고정 6열** (시트 작성 실수로 깨지는 경우는 추후 대응)
- 블록 헤더 날짜 텍스트 형식은 기존과 동일 (`period` 텍스트 재사용)
- "회차 = 주차" — 네이밍은 **회차(session)** 로 통일

**Unit Tests (P0)**
- [x] 다중 블록 파싱: 2개 이상 블록이 빈 열(G, N, ...)을 경계로 독립된 회차로 분리하며 블록별 헤더 4필드(`programName`/`period`/`location`/`totalHours`) 개별 추출 → 01d9aaa
- [x] 미래 회차 필터: 오늘 기준 `period` 시작일이 미래인 블록은 결과에서 제외 → 94ae871
- [x] 첫 번째 탭만 사용: 여러 탭이 반환돼도 서버에서 첫 번째 탭만 파싱해 결과에 반영 (다른 탭 데이터는 학생에게 미노출) → 43e5871

**Implementation**
- [x] `parseSessionBlocks()` 신규 — 6열 + 1열 간격 반복 파싱, 회차 배열 반환
- [x] `period` 텍스트 → 시작일 파싱 유틸 (기존 헤더 텍스트 형식 재사용)
- [x] `extractFirstTabSessions()` 추출: 첫 번째 탭만 파싱 (향후 `parseSessionBlocks` 적용 지점)
- [x] `SessionTabs` 재활용: 입력 소스를 "Sheets 탭 목록" → "파싱된 회차 블록 배열"로 변경
- [x] 탭 버튼은 현재 회차까지만 렌더링 (미래 회차 버튼 없음) → dd18256
- [x] 초기 선택: 오늘이 포함된 회차 탭 (기존 자동 선택 로직 유지)

**Manual Tests**
- [x] 한 탭에서 회차 블록이 분리되어 브라우저에 표시됨
- [x] 1/2회차 탭 클릭 시 해당 회차 시간표로 전환

**완료 조건:** 새 시트 구조에서 현재 회차까지만 탭으로 노출되고, 다른 탭 정보가 응답/UI 어디에도 드러나지 않음

---

### Milestone 17: 실제 시트 연동 및 QA

**목표:** 매니저 실제 시간표 시트와 연동하며 발견되는 레이아웃/구조 차이를 점진적으로 처리한다. 새로운 항목은 운영 중 계속 추가된다.

**작업 로그**

- [x] 실제 시트 레이아웃 적용 (첫 행 파싱 대상 제거)
- [x] 매니저 시트 연결 → 5개 회차가 블록별로 분리되어 파싱됨
- [x] 병합 누락 자동 보정 — 빈 텍스트 + 위 셀과 같은 배경색이면 가상 merge로 처리 (매니저가 병합 빠뜨리고 색만 칠한 케이스) → 8d9e9e4
- [x] 시간표 로딩 스피너 — `VisibleSessionTabs` Suspense fallback에 회전 SVG + "불러오는 중" 라벨 → ec4a9a6
- [x] 미래 회차 필터링 위치 재검토 — 서버 날짜 필터링 vs 클라이언트 필터링 캐싱 모델 문서화 → a6771f9
- [x] 미래 회차 필터링을 클라이언트로 이동 — 시간표 렌더를 `connection()` 동적 홀에서 제거하고 CDN 캐싱 경계로 복귀 → cf9a45d
- [x] 교육 기간 전 접속 시 탭 버튼 없음 (또는 안내 메시지) → cf9a45d
- [ ] 노출 범위를 오늘 기준 이번 회차 + 다음 회차로 한정 (회차 선택 스크롤은 오늘 회차부터 시작되게 고정)

**완료 조건:** 없음 (운영 중 계속 반복)

---

### Milestone 18: 관리자 카테고리 합계 대시보드

📖 TECHSPEC: §1.2 관리자 기능, §6.1 Category, §7.9 관리자 카테고리 합계 대시보드

**목표:** 매니저가 카테고리별 목표(16h 누적) 대비 실제 배치 시수를 한 화면에서 검증하고, 카테고리 클릭으로 시간표 그리드의 배치 위치를 빠르게 확인할 수 있도록 한다.

**배경**
- 매니저는 학생용 페이지를 보지 않음 → 관리자 페이지에 그리드도 함께 노출
- `parseCategories()`가 이미 시트 범례에서 `{name, color}`를 추출 중 → 그대로 재사용
- 목표 시수는 코드 상수로 관리 (16h 고정, 예외 3개 null) — 시트 파싱 변경 없음

**Unit Tests**
- [x] `getCategoryTargetHours`: 기본 카테고리 → 16, 예외(지역맞춤/외부연계/자율) → null → fc64e1e
- [x] 회차×카테고리 시간 합산: 색상 매칭 + rowSpan 누적 + `isMergedContinuation` 제외 검증 → fc64e1e

**Implementation**
- [x] `lib/categoryTargets.ts` — `getCategoryTargetHours(name)` 함수 → fc64e1e
- [x] `lib/categoryStats.ts` — 회차별/카테고리별 배치 시간 합산 로직 → fc64e1e
- [x] `<TimetableGrid>` 에 `highlightCategory?: string` prop 추가 (비매칭 셀 회색 처리) → fdcd35f
- [x] 매트릭스 표 클라이언트 컴포넌트 — 행 클릭 = 카테고리 선택, 정렬(부족분 큰 순 → 구분선 → 목표 없음) → fdcd35f
- [x] `app/admin/categories/page.tsx` — 매트릭스 표 + 그리드 통합 (Server Component, 회차 탭은 클라이언트 상태) → fdcd35f
- [x] `/admin` 페이지에서 `/admin/categories`로 이동하는 링크 추가 → fdcd35f

**Manual Tests**
- [x] 매트릭스 표에 모든 카테고리 노출 + 회차별/합계 시간이 시트 내용과 일치
- [x] 카테고리 행 클릭 → 그리드에서 해당 카테고리 셀만 원색 유지, 나머지 회색
- [x] 회차 탭 전환 시에도 강조 상태 유지
- [x] 다시 같은 행 클릭 시 강조 해제, 그리드 원래대로 복귀
- [x] 목표 없는 카테고리(지역맞춤/외부연계/자율)는 합계만 표시되고 부족분 음수가 안 나옴

**완료 조건:** 관리자 페이지에서 카테고리별 목표 대비 배치 시수를 확인 + 클릭으로 시간표 그리드에서 배치 위치를 빠르게 식별 ✅

---

### Milestone 19: 1차 QA 정리 (성능)

**목표:** 캐시 미스 시 첫 로딩 ~10초 병목을 해소한다.

**배경**
- 캐시 미스 시 첫 로딩 ~10초 — `includeGridData: true`로 전체 셀/포맷을 끌어와 응답이 수 MB → `notes/problem-solving/sheets-fetch-slow-on-cache-miss.md` 참고

**작업 로그**

- [x] Sheets API 응답 슬림화 — `sheets.spreadsheets.get`에 `fields` 마스크 적용해 parser가 실제 사용하는 필드만 수신 (`notes/problem-solving/sheets-fetch-slow-on-cache-miss.md` 참고) → 0b86713
- [x] Sheets API `ranges` 추가 — A1:AI100으로 사용 영역만 요청. fields 마스크 후 3,397→969ms 추가 단축, LCP 0.4s 도달 → e587731
- [x] before/after 측정 결과를 노트에 기입 (fetch ms, LCP, Performance 점수) → 노트 "결과" 섹션 작성 (fetch 9,996→969ms -90%, LCP 10.46s→0.4s -96%)

**완료 조건:** 캐시 미스 시 fetch 시간이 baseline 대비 측정 가능하게 단축됨 ✅

---

### Milestone 20: 장소/강사 보조 테이블 파싱 + 시간표 표시

**목표:** 시트 하단 보조 테이블(시트 행 52~61)의 장소/강사 정보를 파싱하고, 학생이 시간표에서 수업별 장소/강사를 확인할 수 있게 한다.

**배경**
- 매니저가 시트 행 52~61에 날짜별 장소/강사 보조 테이블을 추가함 (현재는 자유 텍스트라 미파싱)
- 파싱 가능한 5열 형식으로 시트를 수정하기로 협의됨 (아래 선행 조건)
- 앱은 셀 텍스트를 해석하지 않고 그대로 표시한다 — 장소만/강사만/둘 다 모두 동일하게 처리

**선행 조건 (매니저 시트 수정 — 완료 전 구현 착수 금지)**
- [x] 보조 테이블을 블록별 5열로 재작성: `날짜 │ 13:00 │ 14:00 │ 15:00 │ 16:00` (1~4회차 완료, 5회차는 추후)
  - 헤더 행(시트 행 52)에 각 열의 시작 시간 표기
  - 셀 값 = 그 시간에 **시작하는** 수업의 장소/강사 텍스트 (시간 보정·비고 금지)
  - 2시간 수업은 시작 시간 열에만 기입 (병합 슬롯은 시작 시간으로 조회)
  - 날짜는 본 그리드와 동일한 `M/D` 표기 (조회 키)
  - 진행: 1~4회차 완료·검증됨(2026-06-14). 5회차는 형식 미정(헤더가 범위 표기 + 내용 미입력) → 추후 별도 처리. 데이터 없는 슬롯은 정상 동작해야 함

**Unit Tests**
- [x] 보조 테이블 파싱: (날짜 × 시작 시간) → 장소/강사 텍스트 조회 테이블 생성 — Commits: 317c287
- [x] 본 그리드 슬롯 매칭: 날짜 + 슬롯 시작 시간으로 조회, 없으면 null — Commits: 317c287

**Implementation**
- [x] `lib/` 보조 테이블 파서 — 헤더 행의 시작 시간을 읽어 열 매칭 (열 위치 하드코딩 금지) — Commits: 317c287
- [x] 슬롯 → 장소/강사 조회 연결: 본 그리드 Slot에 장소/강사(없으면 null) 부착 — Commits: 317c287
- [x] 바텀시트 UI — 셀 탭 시 하단에서 슬라이드업, 수업명/장소/강사 표시. 시트가 떠 있는 채로 다른 셀 탭하면 내용 교체, 바깥/닫기로 해제 (모바일·데스크톱 공통) — Commits: 1990bc8
- [x] 탭 가능 힌트 — 장소/강사 정보가 있는 셀에 표시(예: 📍 아이콘) — Commits: 1990bc8
- [x] 예외 처리 — 정보 없는 슬롯(5회차 미입력·자율수업 등)은 시트 미표시 또는 "정보 없음" — Commits: 1990bc8

**완료 조건:** 학생이 시간표에서 수업을 클릭하면 해당 수업의 장소/강사가 표시된다

---

### Milestone 21: 지난 주차 시간표 아래로 정렬 + 구분선

**목표:** 회차 내에서 마지막 수업일이 지난 주차를 시간표 맨 아래로 내려, 사용자가 스크롤 없이 현재·다가오는 주차를 먼저 보게 한다.

**배경**
- 같은 회차에서 5주차를 보려면 스크롤을 많이 내려야 한다는 사용자 의견
- 마지막 수업일이 지난 주차를 아래로 보내고, 안 지난 주차 그룹과 구분선으로 분리한다
- 예) `1·2·3·4·5` → 1주차 종료 후 → `2·3·4·5 │ 1` → 2주차 종료 후 → `3·4·5 │ 1·2`
- 각 그룹 내부는 주차 오름차순 유지

**판정 기준**
- "지난 주차" = 그 주차에서 제목 있는 슬롯이 있는 날(실제 수업일)의 마지막 날짜 `< 오늘` (날짜 단위, 시·분 무시)
- 마지막 수업일 당일은 지나지 않은 것으로 본다 (그날 종일 위에 남고 자정부터 아래로)
- 연도 보정은 기존 `getSessionRange`와 동일 규칙 재사용 (12월→1월 회차)

**Unit Tests**
- [x] 마지막 수업일이 지난 주차는 past, 안 지난 주차는 upcoming으로 분리하고 각 그룹은 주차 오름차순 유지 — Commits: 91c995c
- [x] 마지막 수업일 당일은 past로 가지 않는다 (`< 오늘` 경계) — Commits: 91c995c

**Implementation**
- [x] `lib/session.ts` — `weekClassDates` 헬퍼 추출, `getSessionRange`가 재사용하도록 교체 — Commits: 91c995c
- [x] `partitionWeeksByRecency(session, today)` → `{ upcoming, past }` — Commits: 91c995c
- [x] `TimetableGrid` — upcoming 렌더 → upcoming·past 둘 다 있으면 구분선(`지난 주차`) → past 렌더로 교체 — Commits: 91c995c
- [x] 모든 주차가 지난 회차는 구분선 없이 순서대로 표시 — Commits: 91c995c

**완료 조건:** 마지막 수업일이 지난 주차가 시간표 하단으로 이동하고, 지난·현재 주차가 구분선으로 나뉜다 ✅

---

### Milestone 22: 토더기 오늘 진행도 위젯

**목표:** 화면 우측 하단에 토더기 캐릭터 플로팅 버튼을 두고, 클릭하면 전 과정 진행도를 보여주는 모달로 학생이 "오늘 어디까지 왔는지"를 한눈에 파악하게 한다.

**배경**
- 전역일 계산기에서 영감 — 친근한 마스코트(토더기)로 동기부여
- 진행도는 특정 회차가 아닌 **전 회차(전 과정) 합산** 기준
- 메인 지표는 이수 시간 진행바("174 / 243시간 함께했어요" + %)

**Unit Tests**
- [x] `computeOverallProgress` — 진행 중/개강 전/수료 후 단계별 진행률·이수시간·남은일수 (전 회차 합산, 병합 연속 셀 시간 제외) — Commits: bfc5452, e15b01d

**Implementation**
- [x] `lib/progress.ts` — 전 회차 수업일을 합산해 이수 시간 기준 진행률과 단계(개강 전/진행 중/수료 후) 계산 — Commits: bfc5452, e15b01d
- [x] 토더기 플로팅 버튼 — 오늘 날짜 표시, 우측 하단 floating (장소 바텀시트와 z-index 분리) — Commits: 8f91915, 26026f6
- [x] 오늘 진행도 중앙 모달 — 클릭 시 popIn 팝업으로 진행바 + 단계별 문구 표시 — Commits: 26026f6

**완료 조건:** 학생이 토더기 버튼을 누르면 전 과정 이수 진행도가 모달로 표시된다 ✅

---

## 🧪 P0 테스트 체크리스트

| 마일스톤 | 테스트 | 상태 |
|---------|--------|------|
| M4-T | RGB(0~1) → hex 변환 정확성 | [x] |
| M4-T | 배경색 없는 셀 → #ffffff | [x] |
| M4-T | 병합 셀 rowSpan 값 정확성 | [x] |
| M4-T | 병합 연속 셀 isMergedContinuation | [x] |
| M4-T | 헤더 영역 파싱 정확성 | [x] |
| M4-T | 빈 셀 처리 | [x] |
| M11 | PIN 일치 시 쿠키 설정 + 200 응답 | [x] |
| M11 | PIN 불일치 시 401 응답 | [x] |
| M12 | 관리자 비밀번호 일치 시 200 응답 | [x] |
| M12 | 관리자 비밀번호 불일치 시 401 응답 | [x] |
| M16 | 다중 블록 파싱 (구분 열 인식 + 블록별 헤더 4필드) | [x] |
| M16 | 미래 회차 필터 | [x] |
| M16 | 첫 번째 탭만 사용 | [x] |
